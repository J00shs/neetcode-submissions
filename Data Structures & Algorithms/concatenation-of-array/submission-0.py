class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        ans = []
        # Fetch every value, add it to ans?
        for i in nums:
            ans.append(i)
        
        for i in nums:
            ans.append(i)

        return ans
